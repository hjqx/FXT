```py
[          "Made by zqh", "Project FXT"
    "i tried making new type of obfuscation xD"
              "difficulty : [33/100]",
]


(*f,d,
) = (
    lambda _: list(
        map(chr,[(r)for (r) in range([[*f,].__class__(f)for (
            f) in (map(range, (0, 105)))][-1][-1],108,2,)],))
)( k:= lambda:  getattr)

(f := f.__add__([d,])) and (
        n := lambda r: k()('',"join")(
            r)) and (
    f := n(f)
)
(r := f) and (
    f := (n)(
        (lambda f, r: n(list(map(chr, [].__class__((r, f))))))(
            (d := list(map(lambda x: x ^0x20, [sum(list(map(
                        lambda g: g *0x2, [40, 0x4])))])
                ))[0],
            (w:=(lambda q,v,f 
            : (v(
                [(lambda:int(ord(q)^f[0]))(
                                                            
)]))[0])("\x57",bytes,(38,(o := lambda g,f,j: 
        k()(globals(),(lambda f,g,b,: b[::-1]   +  g %"e"+f)
                ("r",
                "%sa",
                "lc",)))))),

        )
    )
)
(n := lambda n, g: g("".join(list(map(str, n)))))
(z := (r, f)) and (g := lambda r, n, f: [print, getattr])
(
    r,f,b,) = (
    r := (n := lambda: list(
            map(
                lambda *n: (n)[0][::-1],
                (lambda f, d: ((x := g)(r, f, n).pop())(f % "7x", d)("\x73"))(
                    "86x0sa%s0s17x0",
                    "split",),))
    )()
);((r := 
    (r := (n :=
         lambda n: 
        list(map(lambda f: 0x37.__class__(f, 0x10), (n))))((r, b, f))
        )
    )and (
        ((n := lambda: map(
                chr, list(list(map(int, (f := list(f := map(str, r))[::-1]))))))
            and (
                x := lambda n: list(
                    zip(
                        [x for x in (zip(z, [].__class__(n)))],
                        [n[r] for r in range(len(n)) if r == 0x1 or r == 0x2],
                    )))
        )
    )
) and (
    f := (
        lambda g: (
            (s := x(list(n())))
            and ((e := s[0][0]) and (r := s[1][0]))
            and ((lambda r: (r.insert(2, s[0x1][0x1]), r)[0x1]))(
                v := ((list(map(lambda x: list(x), (r := (list(list(zip(e, r)))))))))[1]
            )
            and (g(list(map("".join, list(zip(e, [r[0][1], "".join(v[1:])]))))))
        )
    )(
        g(r, (n, f), b)[0],
    ),
    ((f := o)(
        g, b, r)),
)
```
